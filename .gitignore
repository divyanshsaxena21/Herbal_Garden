# Ignore node_modules at the root level (just in case)
node_modules/

# Ignore log files in the root (though they should mostly be inside frontend/backend)
*.log

# Ignore system-specific files
.DS_Store
Thumbs.db

# Ignore IDE and editor specific files
.vscode/
.idea/

# Ignore environment files if present at the root (e.g., .env)
.env
.env.local
.env.*.local
